{
  "name": "gitpeek-root",
  "version": "1.0.0",
  "description": "Full Stack GitHub Explorer Dashboard App",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix frontend",
    "server": "npm run dev --prefix server",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx -y concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\""
  }
}
